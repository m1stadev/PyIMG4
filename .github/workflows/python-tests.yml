# This workflow will install Python dependencies abnd run tests

name: Run tests

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13"]
        asn1-version: ["asn1<3", "asn1>=3"]

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Install uv and Python ${{ matrix.python-version }}
      uses: astral-sh/setup-uv@v5
      with:
        python-version: ${{ matrix.python-version }}
        enable-cache: true

    - name: Install the project
      run: uv sync --dev

    - name: Install python-asn1 (${{ matrix.asn1-version }})
      run: uv pip install '${{ matrix.asn1-version }}'

    - name: Run tests
      run: uv run --no-sync pytest
